# 📝 Development Log: 2026-09-02 — Release 1.1.2 (Fix Windows WebView2Loader.dll Runtime Resolution)

## 📋 ภาพรวมการดำเนินงาน
บันทึกการแก้ไขปัญหาประจำวันสำหรับ Release `v1.1.2` (SemVer Patch) เพื่อแก้ไขปัญหาตัวติดตั้ง NSIS ที่วางไฟล์ `WebView2Loader.dll` ไว้ในโฟลเดอร์ `$INSTDIR\resources\` ซึ่งทำให้ตัวไฟล์ `.exe` ไม่สามารถโหลด DLL ขึ้นมาทำงานได้เนื่องจากข้อจำกัดของ Windows Dynamic Linker Search Order

---

## 🛠️ รายละเอียดการเปลี่ยนแปลง (Engineering Changes)

### 1. Dual-Layer Defense Strategy สำหรับ WebView2Loader.dll
1. **Tauri Bundle Resource Mapping (`tauri.conf.json`)**:
   - ปรับปรุงการตั้งค่า `bundle.resources` จาก Array เป็น Object/Map syntax:
     ```json
     "resources": {
       "resources/WebView2Loader.dll": "WebView2Loader.dll"
     }
     ```
   - เพื่อให้ Tauri Bundler และ NSIS ทำการคัดลอก `WebView2Loader.dll` มาวางไว้ที่ Root Directory (`$INSTDIR\`) เคียงข้างตัว `.exe` โดยตรง

2. **Native Win32 `SetDllDirectoryW` Runtime Fallback (`main.rs`)**:
   - เพิ่มคำสั่ง Win32 API ผ่าน FFI Zero-overhead ในจุดเริ่มต้นของฟังก์ชัน `main()`:
     ```rust
     #[cfg(target_os = "windows")]
     {
         if let Ok(mut exe_dir) = std::env::current_exe() {
             exe_dir.pop();
             let resources_dir = exe_dir.join("resources");
             if resources_dir.exists() {
                 use std::os::windows::ffi::OsStrExt;
                 let wide: Vec<u16> = resources_dir
                     .as_os_str()
                     .encode_wide()
                     .chain(Some(0))
                     .collect();
                 extern "system" {
                     fn SetDllDirectoryW(lpPathName: *const u16) -> i32;
                 }
                 unsafe {
                     SetDllDirectoryW(wide.as_ptr());
                 }
             }
         }
     }
     ```
   - ฟังก์ชันนี้จะตรวจสอบและเพิ่มโฟลเดอร์ `resources/` เข้าไปยัง DLL Search Directory ของ Process โดยอัตโนมัติ ทำให้ไม่ว่าไฟล์ DLL จะวางอยู่ข้างนอกหรือข้างในโฟลเดอร์ย่อย ตัวโปรแกรมจะค้นหาเจอและรันได้อย่างราบรื่น 100%

### 2. SemVer 2.0.0 Version Bumping (1.1.2)
- `package.json` -> `1.1.2`
- `src-tauri/Cargo.toml` -> `1.1.2`
- `src-tauri/tauri.conf.json` -> `1.1.2`

---

## 🧪 การตรวจสอบคุณภาพ (5-Pillar Quality Gates)
- [x] **Lint**: `npm run lint` (ESLint v9 flat config) — ผ่าน 0 errors
- [x] **Format**: `npm run format:check` (Prettier) — ผ่าน 0 errors
- [x] **Typecheck**: `npm run typecheck` (`tsc --noEmit`) — ผ่าน 0 errors
- [x] **Unit Tests**: `npm test` (Vitest test suites) — ผ่าน 100%
- [x] **Build Integrity**: `npm run build` (Vite production bundle) — ผ่าน 100%
- [x] **Desktop Release Artifacts**: รวบรวมไฟล์ติดตั้งไว้ที่ `releases/v1.1.2/`
  - `lofi-bun-companion_1.1.2_x64-setup.exe` (NSIS Setup Installer)
  - `lofi-bun-companion_1.1.2_x64_en-US.msi` (MSI Installer)
  - `lofi-bun-companion.exe` (Standalone Executable)
  - `WebView2Loader.dll` (Embedded Runtime DLL)

